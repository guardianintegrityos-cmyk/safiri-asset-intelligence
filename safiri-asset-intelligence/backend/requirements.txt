# FastAPI web framework & ASGI server
fastapi>=0.111.1
uvicorn[standard]>=0.28.0
starlette>=0.37.0

# Pydantic data validation & config management
pydantic>=2.6.0
pydantic-settings>=2.3.0
pydantic-core>=2.16.0

# Data processing & ML
numpy>=1.26.0,<2.0
scikit-learn>=1.3.0
pandas>=2.1.0

# Database & ORM
SQLAlchemy>=2.0.22
psycopg2-binary>=2.9.0
neo4j>=6.1.0
alembic>=1.12.0

# Elasticsearch
elasticsearch>=8.11.0

# Security & authentication
PyJWT>=2.8.0
cryptography>=41.0.0
bcrypt>=4.1.0

# Rate limiting & anti-scraping
slowapi>=0.1.9

# Data validation & masking
phonenumbers>=8.13.0

# Fuzzy matching / string processing
rapidfuzz>=3.14.0

# HTTP / async utilities
httpx>=0.27.0
requests>=2.32.0

# Environment & config
python-dotenv>=1.0.0

# Email and SMS services
email-validator>=2.0.0
twilio>=8.0.0

# Document validation (for forgery detection)
# Note: For production, install system libmagic: apt-get install libmagic1 (Linux) or brew install libmagic (macOS)
python-magic>=0.4.24
Pillow>=10.1.0

# Logging and monitoring
loguru>=0.7.0
